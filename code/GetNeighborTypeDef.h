#ifndef GETNEIGHBORTYPEDEF_H
#define GETNEIGHBORTYPEDEF_H

#include "DBSCAN.h"
#include "sampleSort.h"

// this was used to also define a grid data structure for integer based grids, now deprecated; this can still be used to define other grid impls with the same interface, see Grids.h.
#include "Grids.h"
typedef struct Grids GridsGN;

#endif
